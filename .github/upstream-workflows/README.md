# Archived upstream automation

These workflows are retained for upstream comparison but are outside GitHub's executable workflow directory. They include Anomaly deployment, publishing, repository triage, and other infrastructure that this fork does not operate.

Only the workflows under `.github/workflows/` should run in this fork. New upstream workflows require explicit review during integration. Do not move these back to the executable directory as a shortcut for releasing the product.
